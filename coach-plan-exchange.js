(function(){
  'use strict';
  const model=window.rcCoachPlanExchangeModel,store=window.rcDataStore,panel=document.getElementById('coach-exchange-panel');if(!model||!store||!panel)return;
  const status=document.getElementById('coach-exchange-status'),current=document.getElementById('coach-exchange-current'),feedback=document.getElementById('coach-exchange-feedback'),fileInput=document.getElementById('coach-plan-file'),modal=document.getElementById('coach-plan-modal'),preview=document.getElementById('coach-plan-preview'),errorBox=document.getElementById('coach-plan-error'),confirmButton=document.getElementById('coach-plan-confirm');
  let pending=null;
  function data(name,fallback){try{return store.getDataset(name)??fallback;}catch(_){return fallback;}}
  function today(){const date=new Date();return`${date.getFullYear()}-${String(date.getMonth()+1).padStart(2,'0')}-${String(date.getDate()).padStart(2,'0')}`;}
  function fmt(value){return new Date(`${value}T12:00:00`).toLocaleDateString('it-IT',{day:'numeric',month:'short',year:'numeric'});}
  function escapeHtml(value){return String(value??'').replace(/[&<>'"]/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[char]));}
  function toast(message){const node=document.getElementById('toast');node.textContent=message;node.classList.add('show');setTimeout(()=>node.classList.remove('show'),2200);}
  function activeGoal(){return model.activeGoal(data('goals',[]),today());}
  function activePlan(goal){return data('coachPlans',[]).filter(item=>item.goalId===goal?.id&&item.status==='active').sort((a,b)=>b.revision-a.revision)[0]||null;}
  function render(){
    const goal=activeGoal(),plan=activePlan(goal);document.getElementById('coach-brief-export').disabled=!goal;document.getElementById('coach-plan-import').disabled=!goal;
    status.className=`coach-exchange-status ${plan?'active':'waiting'}`;status.textContent=plan?`REVISIONE ${plan.revision} ATTIVA`:'DA PREPARARE';
    if(!goal){current.innerHTML='<strong>Nessun obiettivo A futuro</strong><span>Aggiungi prima la gara principale.</span>';return;}
    if(!plan){current.innerHTML=`<strong>${escapeHtml(goal.name)} · nessun piano Codex importato</strong><span>La generazione interna resta disponibile come provvisoria, ma non sostituisce una programmazione completa revisionata.</span>`;return;}
    const count=(plan.sessionIds||[]).length;current.innerHTML=`<div><small>PIANO ATTIVO</small><strong>${escapeHtml(plan.goalName)} · revisione ${plan.revision}</strong><span>${fmt(plan.validFrom)} – ${fmt(plan.validTo)} · ${count} sedut${count===1?'a':'e'}</span></div><div><small>METODO</small><span>${escapeHtml(plan.methodology.summary)}</span></div>`;
  }
  function brief(){return model.createBrief({profile:data('profile',{}),hrZones:data('hrZones',null),sessions:window.rcSessions?.getAll?.()||data('sessions',[]),retiredPlanSessions:data('retiredPlanSessions',[]),weeklyAvailabilityHistory:data('weeklyAvailabilityHistory',[]),preSessionCheckins:data('preSessionCheckins',[]),bodyIssues:data('bodyIssues',[]),importedActivities:data('importedActivities',[]),whoopCycles:data('whoopCycles',[]),whoopSleeps:data('whoopSleeps',[]),whoopWorkouts:data('whoopWorkouts',[]),goals:data('goals',[]),coachPlans:data('coachPlans',[])});}
  function download(value,name){const blob=new Blob([JSON.stringify(value,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),link=document.createElement('a');link.href=url;link.download=name;link.click();setTimeout(()=>URL.revokeObjectURL(url),0);}
  function openPreview(result){
    pending=result;errorBox.hidden=true;errorBox.textContent='';confirmButton.disabled=false;const item=result.preview,categories=Object.entries(item.categoryCounts).map(([key,value])=>`<span>${escapeHtml(key)} <b>${value}</b></span>`).join('');
    preview.innerHTML=`<section class="coach-plan-preview-hero"><small>${escapeHtml(item.goalName)} · REVISIONE ${item.revision}</small><strong>${fmt(item.validFrom)} – ${fmt(item.validTo)}</strong><p>${escapeHtml(item.methodology)}</p></section><div class="coach-plan-preview-metrics"><span><b>${item.added}</b><small>Nuove sedute</small></span><span><b>${item.replaced}</b><small>Base sostituite</small></span><span><b>${item.protected}</b><small>Voci protette</small></span></div><div class="coach-plan-preview-categories">${categories}</div>${item.warnings.length?`<ul>${item.warnings.map(warning=>`<li>${escapeHtml(warning)}</li>`).join('')}</ul>`:''}<p class="coach-plan-guardrail">Sedute svolte, passato, modifiche manuali e gare già presenti restano protetti. L’adattamento settimanale lavorerà su questa progressione.</p>`;
    modal.classList.add('open');modal.setAttribute('aria-hidden','false');
  }
  function close(){pending=null;modal.classList.remove('open');modal.setAttribute('aria-hidden','true');fileInput.value='';}
  function context(){return{today:today(),goal:activeGoal(),goals:data('goals',[]),sessions:window.rcSessions?.getAll?.()||data('sessions',[]),coachPlans:data('coachPlans',[]),retiredPlanSessions:data('retiredPlanSessions',[])};}
  document.getElementById('coach-brief-export').addEventListener('click',()=>{try{const value=brief(),slug=String(value.objective.primary.name||'obiettivo').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');download(value,`re-carlo-v-coach-brief-${slug}-${value.today}.json`);feedback.textContent='Dossier pronto: allegalo nella task “Re Carlo V — Elite Coach”.';toast('Dossier Coach esportato');}catch(error){feedback.textContent=error.message;}});
  document.getElementById('coach-plan-import').addEventListener('click',()=>fileInput.click());
  fileInput.addEventListener('change',async()=>{const file=fileInput.files?.[0];if(!file)return;feedback.textContent='Controllo del piano in corso…';try{const raw=await file.text(),result=model.buildImport(raw,context()),enriched=window.rcSessionPrescriptionModel?.enrichSessions?.(result.sessions,{profile:data('profile',{}),goals:data('goals',[]),hrZones:data('hrZones',null)})?.sessions||result.sessions,byId=new Map(enriched.map(item=>[item.id,item]));result.sessions=enriched;result.nextSessions=result.nextSessions.map(item=>byId.get(item.id)||item);feedback.textContent='Piano valido: controlla l’anteprima prima di importarlo.';openPreview(result);}catch(error){const details=Array.isArray(error.details)&&error.details.length?` ${error.details.slice(0,3).join(' ')}`:'';feedback.textContent=`${error.message}${details}`;fileInput.value='';}});
  confirmButton.addEventListener('click',()=>{if(!pending)return;try{confirmButton.disabled=true;store.commitCoachPlan({sessions:pending.nextSessions,coachPlans:pending.nextCoachPlans,retiredPlanSessions:pending.nextRetiredPlanSessions,planId:pending.record.id,revision:pending.record.revision,added:pending.preview.added,replaced:pending.preview.replaced});const detail={reason:'coach-plan-imported',planId:pending.record.id,revision:pending.record.revision};close();document.dispatchEvent(new CustomEvent('rc:sessions-updated',{detail}));document.dispatchEvent(new CustomEvent('rc:coach-plan-updated',{detail}));feedback.textContent=`Revisione ${detail.revision} importata e sincronizzata.`;toast('Piano Coach importato');render();}catch(error){confirmButton.disabled=false;errorBox.hidden=false;errorBox.textContent=error.message;}});
  document.getElementById('coach-plan-close').addEventListener('click',close);document.getElementById('coach-plan-cancel').addEventListener('click',close);
  ['rc:data-restored','rc:sessions-updated','rc:goals-updated'].forEach(name=>document.addEventListener(name,render));render();
})();
